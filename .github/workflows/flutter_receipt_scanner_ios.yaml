name: flutter_receipt_scanner_ios

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    paths:
      - .github/workflows/flutter_receipt_scanner_ios.yaml
      - flutter_receipt_scanner_ios/**
  push:
    branches:
      - main
    paths:
      - .github/workflows/flutter_receipt_scanner_ios.yaml
      - flutter_receipt_scanner_ios/**

jobs:
  build:
    if: ${{ !contains(fromJSON('["dependabot[bot]", "renovate[bot]", "snyk-bot", "allcontributors[bot]"]'), github.actor) }}
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_version: 3.44.6
      format_line_length: 120
      working_directory: flutter_receipt_scanner_ios
